services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: diffusion_app:latest
    container_name: diffusion_app
    stdin_open: true
    tty: true
    # Uncomment and adjust ports if your image exposes a service
    # ports:
    #   - "8000:8000"
